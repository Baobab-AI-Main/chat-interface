/**
 * @deprecated Teams have been retired from the product.
 * This placeholder prevents legacy imports from breaking the build while making
 * the removal explicit to future maintainers.
 */
export function useTeams() {
  throw new Error('useTeams has been removed. Update callers to use the new chat conversation flow.');
}
